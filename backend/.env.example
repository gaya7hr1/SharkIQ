# --- App ---
APP_ENV=development
APP_NAME=SharkIQ
API_V1_PREFIX=/api/v1
SECRET_KEY=change-me-to-a-random-64-char-string
ALLOWED_ORIGINS=http://localhost:3000

# --- Database ---
DATABASE_URL=postgresql+asyncpg://sharkiq:sharkiq@localhost:5432/sharkiq
DATABASE_URL_SYNC=postgresql+psycopg2://sharkiq:sharkiq@localhost:5432/sharkiq

# --- OpenAI (unused by default - kept for easy switch-back, see app/agents/base.py) ---
OPENAI_API_KEY=sk-replace-me
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# --- Groq (chat model provider) ---
GROQ_API_KEY=gsk_replace-me
GROQ_CHAT_MODEL=llama-3.1-8b-instant
# Min seconds between Groq calls (shared by all agents + committee) to stay under
# the free-tier tokens-per-minute cap. Lower if you're on a paid tier, raise if
# you're still hitting 429s.
GROQ_MIN_REQUEST_INTERVAL_SECONDS=16.0

# --- Local embeddings (Hugging Face sentence-transformers, CPU, no API key) ---
HF_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# --- ChromaDB ---
CHROMA_PERSIST_DIR=./storage/chroma
CHROMA_HOST=
CHROMA_PORT=

# --- LangSmith (tracing/debugging for every LangChain & LangGraph run) ---
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=ls__replace-me
LANGCHAIN_PROJECT=SharkIQ
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# --- Hugging Face Hub ---
HUGGINGFACEHUB_API_TOKEN=hf_replace-me
HF_SENTIMENT_MODEL=distilbert-base-uncased-finetuned-sst-2-english

# --- Storage ---
UPLOAD_DIR=./storage/uploads
REPORTS_DIR=./storage/reports
MAX_UPLOAD_SIZE_MB=25

# --- Auth ---
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# --- Logging ---
LOG_LEVEL=INFO
